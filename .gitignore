# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Developer's own SQLite databases (regenerated on first boot)
data/*.db
data/*.db-shm
data/*.db-wal

# Local config — contains absolute, machine-specific paths.
# Copy config.example.yaml to config.yaml and edit for your environment.
config.yaml

# Local Claude Code MCP server registration. Contains absolute paths
# to sibling MCP servers. Copy .mcp.example.json to .mcp.json and edit
# the paths for your environment.
.mcp.json

# Editor / OS noise
.vscode/
.idea/
.DS_Store
*.swp
*.swo
